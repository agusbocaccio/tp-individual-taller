// use std::{fs::File, io::{BufWriter, Write}};

// pub fn write_file(data: Vec<Vec<char>)-> (){
//     let f = File::create("/tmp/foo").expect("Unable to create file");
//     let mut f = BufWriter::new(f);
//     f.write_all(data);
// }