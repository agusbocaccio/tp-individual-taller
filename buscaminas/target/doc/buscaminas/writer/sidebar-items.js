window.SIDEBAR_ITEMS = {"fn":[["show_field","Receives a Cell matrix and prints the field with the * (bombs) and ‘.’ (empty) characters."],["write_data","Receives a Cell matrix and an opened valid File. Translates the Cell field into chars, ‘*’ for bombs and ‘.’ for empty cells and writes it in the file."],["write_file","This function creates a file named ‘out.txt’, or overwrites it if it does already exists and writes  the field with the * (bombs) and ‘.’(empty) characters."]]};